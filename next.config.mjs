/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Allows remote placeholder/real imagery to be swapped in later if needed.
    remotePatterns: [],
  },
};

export default nextConfig;
